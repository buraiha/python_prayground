{
    // A name for the dev container displayed in the UI.
    "name": "Python Praygroundです",
    
    // dockerイメージ設定
    // 開発用言語概ね入りの便利ユニバーサルイメージ: mcr.microsoft.com/devcontainers/universal:linux
    // 軽量イメージ: alpine:latest
    // "image": "mcr.microsoft.com/devcontainers/universal:linux",
    "image": "alpine:latest",
 
    // featuresの設定
    // ユニバーサルコンテナを使用しているが、common-utilsを指定してbuildしてみる。
    "features": {
        "ghcr.io/devcontainers/features/common-utils:latest":{
            "username": "vscodetest",
            "userUid": "1024",
            "userGid": "1024"
        }
        /*
        "ghcr.io/devcontainers/features/python:1.4":{
            "version": "3.12.2"
        }
        */
    },
    
    // 無定義だとrootで動いてしまうので、必要であればユーザを変更する。
    // features/common-utils なりほかの方法なりで、あらかじめ下記で指定したユーザーが作成されている必要がある。
    // mcr.microsoft.com/devcontainers/universal:linuxはcodespaceユーザがデフォルトで作成されるので、指定しておく。
    "remoteUser": "",
    
    // VS codeのカスタマイズ
    "customizations": {
        // Configure properties specific to VS Code.
        "vscode": {
            // Add the IDs of extensions you want installed when the container is created.
            "extensions": [
                "oderwat.indent-rainbow",
                "ms-python.flake8"
            ]
        }
    },
    
    // コンテナが初めて開始された直後にコンテナ内で実行されます。
    // "onCreateCommand": "",

    // 作成プロセス中に新しいコンテンツがソースツリーで利用可能になるたびに、onCreateCommand の後にコンテナ内で実行されます。
    // "updateContentCommand": "",

    // updateContentCommand の後に dev コンテナが初めてユーザーに割り当てられたときに実行されます。
    // 基本的にはここに環境features等で設定する以外のコンテナ特有のスクリプトを記述する。
    // "postCreateCommand": ""

}
